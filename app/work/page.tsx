"use client";
import { useState } from "react";
import { projects } from "../../lib/content";
import { SiteFooter, SiteHeader } from "../../components/marketing";
const filters = ["All", "Photography", "Videography", "Events", "Corporate", "Real Estate", "Campaigns"];
export default function Work() { const [active, setActive] = useState("All"); const visible = active === "All" ? projects : projects.filter(p => p.category === active || p.label === active); return <main><SiteHeader /><section className="archive-head"><p className="eyebrow">Avant Creative Group · Portfolio archive</p><h1>Selected<br /><em>work.</em></h1><p className="body-copy">A collection of moments, campaigns and stories captured by Avant. Filter by the kind of story you need to tell.</p></section><section className="archive-grid-section"><div className="filters">{filters.map(filter => <button key={filter} className={active === filter ? "active" : ""} onClick={() => setActive(filter)}>{filter}</button>)}</div><div className="project-grid archive-grid">{visible.map((p, i) => <a className={`project-card ${p.layout}`} href={`/work/${p.slug}`} key={p.slug}><img src={p.image} alt={p.title} loading="lazy" /><div className="project-overlay"><span>{p.label}</span><strong>{p.title}</strong><small>{p.year} · {p.location}</small></div><div className="project-index">0{i + 1}</div></a>)}</div></section><SiteFooter /></main>; }
